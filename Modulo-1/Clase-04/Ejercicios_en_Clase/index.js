//SUMA
function sum(x, y) {
    return x + y
}
console.log(sum (15,45))

// MULTIPLICACIÓN
let multiplicar = function(a, b){
    return a * b
}
let resultado = multiplicar(5,9)
console.log(resultado)

//Función para calcular el área del triangulo
function  AreaTrinagulo(base, altura){
    let area = (base * altura)/2
    return area
}
let result = AreaTriangulo(10,5)
console.log("El área es: " + result)

//Función que reciba 3 numeros y que saque 1

// Función que pueda recibir el nombre de una persona y saludar

// Crear una funcion que divida dos numeros

//Función que te diga si es par o impar
